---
import Section from '../layout/Section.astro';
import Container from '../layout/Container.astro';
import SectionHeading from '../ui/SectionHeading.astro';
import Button from '../ui/Button.astro';
import { getSubjects } from '../../utils/content/collections';

const subjects = await getSubjects();
---
<Section surface="white" labelledby="subjects-heading" border>
  <Container>
    <div class="grid items-start gap-8 lg:grid-cols-[minmax(280px,0.85fr)_1.6fr] lg:gap-[clamp(32px,5vw,88px)]">
      <div>
        <SectionHeading id="subjects-heading" eyebrow="Subjects">Learn With Purpose</SectionHeading>
        <p class="m-0 mb-6 mt-4 max-w-[44ch] text-[17px] text-ink-mute">
          Every subject hub gathers the topics, study guides and practice material for that subject in one place.
        </p>
        <Button href="/subjects/" variant="quiet">All subjects</Button>
      </div>
      <ul class="m-0 grid list-none p-0 sm:grid-cols-2 sm:gap-x-[clamp(24px,4vw,64px)]">
        {subjects.map((subject) => (
          <li class="m-0 border-b border-rule">
            <a href={`/subjects/${subject.id}/`} class="flex items-baseline justify-between gap-4 py-[17px]">
              <span class="font-display text-2xl tracking-[-0.01em]">{subject.data.title}</span>
              <span class="whitespace-nowrap text-[13px] text-ink-mute">{subject.data.levelsLabel}</span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  </Container>
</Section>
